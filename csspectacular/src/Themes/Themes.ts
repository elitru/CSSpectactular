import { Theme } from "./Theme";
import __theme_default from "./ThemeTemplates/Default";

/**
 * @description all available themes included in the library
 */
export class Themes {
    public static readonly Default: Theme = __theme_default;
};